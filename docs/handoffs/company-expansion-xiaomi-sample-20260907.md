# 小米浏览器样本验证与下一步 — 2026-09-07

最新结论：用户明确允许沿用原方式后，13:32 的 Playwright 样本已观察到一次自然下一页，校园两页共 20 个不同岗位，新零售首屏另有 10 个不同岗位。实习入口在整轮总请求到限时中断；整体实验未通过完整验收、新增准入仍为 0。详见文末“原有浏览器读取方式的后续验证”。

## 11:44 首轮结果（历史）

用户在接收企业扩容交接、修订四家后续措施并评估新 `web-crawler` MCP 后，明确要求继续项目，并将 MCP 优化建议传给任务 `01a06503-3fc6-7351-9342-88e27bcd48dd`。
本轮执行小米单次、受控的浏览器样本实验。**新增正式接入仍为 0 家；没有证明自然翻页、全量采集或项目完整覆盖。**
没有修改业务数据库、迁移、生产采集器、云端代码、快照或计划任务，没有提交或推送生产部署。当前任务工作树仍为 `8c2c399`，本轮追加文件在原项目 `E:\devlop\official-campus-radar` 的 `codex/wechat-oa-integration` 分支完成；未切换、覆盖或 cherry-pick 当前工作树。

机器记录：`work/company-expansion-xiaomi-browser-sample-20260907.json`。
开始时间：2026-09-07 11:44:05 北京时间；耗时 10.58 秒；本机实验进程及其子进程采样峰值 RSS 为 525.2 MiB，不能替代 ECS 全量任务的内存验收。
命令退出码 **1**，停止原因为 `endpoint_budget_exhausted`，不是官网封禁或网站不可采。

## 已取得的事实

- `/campus/` 返回 HTTP 200，保留岗位首屏 10 条，10 个唯一 `id`，接口及页面声明总数 767。首屏中 4 条为 `recruit_type.id=202`（实习），6 条为 `201`（正式）；两者上级都是 `2`（校招）。
- 同一校园页的招聘项目筛选列出“2027届境外招聘计划”“2027届校园招聘计划”“实习生招聘计划”。因此不能把 `/campus/` 整体当成一个互斥批次，也不能把导航入口名称直接当成项目身份。
- 岗位响应包含 `job_subject`、`job_process_id`、`process_type` 等字段名，但本轮未保留其值。它们只是后续稳定项目身份核验的线索，不能猜测映射。
- 页面真实下一页是 `li.atsx-pagination-next`，标题“下一页”；另有“向后 5 页”的跳页控件。当前实验只点击可明确识别的单个 `a/button`，因此没有匹配到安全点击目标，实际翻页点击 **0 次**。不能把这个选择器限制解释成官网无法翻页。
- `/newretailing/` 返回 HTTP 200，页面列出“2027届新零售招聘计划”，并显示零岗位，但实验在该页面受本地预算中断、没有保留有效岗位响应，所以 **不能判定项目关闭**。
- `/internship/` 未打开。三入口全覆盖没有完成。

## 预算与停止

冻结计划为三个已知入口各一次导航，校园页最多一次自然下一页，任务时限 180 秒；无重放、无重试。新建临时浏览器 context，不读取个人 profile、Cookie、请求头、签名或 CSRF 值；自然页面可以生成自己的请求，但不导出或复现其临时状态。显式启用 HTTPS 证书校验，不使用代理。

所有放行的 XHR/fetch 按去查询参数后的 method + host + path 记录，单端点累计上限 6。已知岗位端点继承前轮一次自然捕获：

| 端点 | 既有已知次数 | 本轮放行次数 | 累计 | 后续剩余额度 |
| --- | ---: | ---: | ---: | ---: |
| `POST xiaomi.jobs.f.mioffice.cn/api/v1/search/job/posts` | 1 | 3 | 4 | 2 |
| `POST mcs.snssdk.com/v1/list` | 本轮从 0 计 | 6 | 6 | 0 |

岗位端点本轮放行 3 次，只保留 1 份有效岗位首屏响应；请求已发出、结果未保留的两次仍计入预算。机器记录包含其他配置与遥测端点的本轮计数；这些端点的旧轮历史不完整，不宣称已经重建全历史账本。

停止由页面自动产生的第三方遥测端点达到上限触发。该端点第 7 次请求在放行前被拦截，随后停止整家公司，跳过实习入口。没有通过换进程、换入口或重跑重置额度。

这是实验预算口径过宽造成的提前停止，说明需要区分业务接口、配置和遥测，同时保留全部请求审计。此结论不授权修改既有台账、补发请求或重新执行本轮命令。

## MCP 协作与下一步

已向用户指定的任务“判断 Pi agent 产品定位”发送通用改进建议和本次事实：访问限制立即停止，浏览器人机验证/登录墙检测，跨调用端点预算，实际请求与页面计数分离，页面上限与真正末页分开报告，以及覆盖浏览器抓取阶段的任务截止时间。MCP 项目独立负责修复；招聘雷达负责公告、项目映射和准入，不向爬虫中加入招聘业务模型。

下一次小米实验前先取得并核对通用工具修复结果，再明确剩余请求预算与下一页控件契约。不能因工具升级或换运行环境重新获得 6 次额度；当前共享岗位端点只剩 2 次，显然不足以覆盖约 77 页及其他入口。全量实验需要另外明确请求预算、时间盒、资源上限和未完成状态，不能拼接多个 20 页 MCP 调用冒充已获全量授权。

在取证完成前保持未准入；实验成功也不自动扩大 ADR 0004，生产适配器和部署仍需单企业 ADR 与明确批准。

四家后续队列继续按用户修订后的措施执行，不挤占小米优先级：

| 企业 | 措施 | 本轮冻结上限与退出线 |
| --- | --- | --- |
| 华为 | 从现行官网定位岗位与公告/通知入口，不再投入旧 `reccampportal` 配置字典 | 20 分钟、最多 3 次页面导航、岗位/候选接口请求最多 6 次；到限记录缺口 |
| 阿里 | 仅检查官方校招首页及直接招聘导航，不重放已受限请求 | 20 分钟、导航一轮且最多 3 次、新候选接口最多 6 次；无第二岗位入口即停止，转浏览器方案或保持未接入 |
| 字节 | 核对官方明确提供的其他入口，否则进入浏览器实验方案 | 20 分钟、最多 2 次页面导航、新候选接口最多 6 次；验证码、滑块、登录要求立即停止；可采实验与生产 ADR 分开 |
| 中国移动 | 分别定位校招、实习实际岗位入口，另行授权后正常 TLS 的 ECS 最小复验 | 20 分钟、每入口最多 1 次页面加 1 次候选接口，本机/ECS 共用额度；禁止再请求已超预算无障碍端点，禁止降低 TLS 安全 |

所有企业继承端点历史台账，单企业岗位/候选接口本轮累计不超过 6 次，遇到 401/403/412/429 即停止公司。后续浏览器实验另行明确范围，不恢复微信路线。

## 本批离线验证

Python 3.13 完整测试 645 项，643 通过、2 项平台跳过；新增实验工具与冻结报告回归 8 项通过。`manage.py check`、`makemigrations --check --dry-run`、`git diff --check` 均通过。未重跑真实实验，也未把通过的离线测试当成企业采集验收。

## 新版 MCP 已加载及普通 HTML 检查

用户重启后，本任务工具定义已实际包含 `endpoint_limit`、`per_endpoint_limit` 和 `business_endpoint_limits`，与磁盘新版一致。上游的预算、受限任务禁止恢复及新 schema 已完成接入核对；跨独立调用的预算仍由招聘雷达汇总，不能靠新建 run 重置。

只执行了一次 `inspect_page`：`fetch_mode=http`、总额度 2、每端点额度 1，岗位 API 与已耗尽的遥测端点显式配置为 0。实际请求为 GET `/robots.txt` 一次、GET `/campus/` 一次；HTTP 200，标题“小米校园招聘”，正文只有空的 `div#bd`，候选链接 0。`inspection_status=complete` 仅表示这次 HTML 检查完成，不能解释为岗位采集完成。

机器记录：`work/company-expansion-xiaomi-mcp-http-20260907.json`。本次没有启动浏览器，没有岗位接口、遥测或重放请求。共享岗位端点已知累计仍为 4/6，剩余 2 次；此前遥测端点仍为 6/6，剩余 0。普通 HTML 无法补齐岗位或下一页 DOM 选择器，所以继续需要浏览器及新的明确预算。

本批验证：Python 3.13 完整测试 646 项，644 通过、2 项平台跳过；工具与冻结报告回归 9 项通过；项目检查、迁移漂移检查和差异检查通过。

## 下一轮小米样本计划（用户已批准，执行结果见后）

此计划提出后，用户明确回复“批准”。授权仅覆盖以下受控样本；旧台账和失败记录均保留。不是全量采集，也不是生产适配器/部署批准。

- 范围：仍限已核验的 `/campus/`、`/newretailing/`、`/internship/` 三个入口。最多三个单页检查，加校园入口的一次两页抓取，总计最多 5 个逻辑页面。只自然点击一次明确的下一页，不跳页，不修改页面请求参数。
- 新增岗位端点额度：最多 8 次；加既有已知 4 次，累计上限变为 12。每次调用从剩余额度分配，不能为各调用重复分配 8 次。
- 新增全部网络请求：合计最多 200 次，包含 robots、页面、静态资源、配置、遥测、岗位接口、失败和在途请求；其他每个 host/path 的新增额度最多 20 次。旧计数作为历史保留，新增预算分别记录，不能改写成旧轮未花费。
- 时间盒：整个新实验最多 20 分钟。开始每次调用前，剩余时间必须足以覆盖该调用的任务时限，否则不再启动新调用。每个 MCP 调用仍保持自己的页数与端点硬上限；所有调用的总和受上述更小的整轮限制。
- 先检查实际 DOM，再冻结岗位 CSS/XPath 与唯一下一页控件；没有唯一键或选择器证据就停止，不猜字段。复用公开页面自然生成的会话，不导出或复现签名/CSRF，不导入个人资料或凭据。
- HTTP 401/403/412/429、明确验证码/滑块/登录墙立即停止企业，不续跑，不开新 run 绕过。任何额度耗尽、时间到限、分页不变或结构不明也停止并记录。
- 输出：实际请求台账、采集页数、岗位唯一键与跨页差异、三个入口中的项目标签/稳定身份候选、当前页面开放信号、耗时与可获得的资源记录。不能把页面零岗位或 `navigation_exhausted` 单独作为正式准入证据。
- 成功判据只限“完成受控样本和至少一次自然翻页验证”；767 条及约 77 页的完整抓取不在此计划内，项目完整覆盖、公告和招聘对象的官方证据以及生产单企业 ADR 仍是后续门槛。

## 批准后执行结果：MCP 单次检查额度耗尽

2026-09-07 13:17:13—13:17:17 北京时间，仅执行一次 `inspect_page(fetch_mode=browser)`，耗时约 4 秒（调用前后 UTC 时钟为秒级）；MCP 未提供内存采样，不能沿用旧工具的峰值作为此次结果。

机器记录：`work/company-expansion-xiaomi-mcp-browser-20260907.json`，包含批准范围、精确调用参数、完整脱敏 MCP 结果和整轮汇总。

校园首屏尚未返回 DOM 就触及 MCP **单次 inspect 上限 50**，状态为 `endpoint_budget_exhausted`。这是工具额度造成的实验中断；没有返回 HTTP 状态、正文、岗位响应或候选链接，不能证明官网受限，也不能推定已取得有效岗位数据。本轮未完成样本、自然翻页或三个入口检查，新增准入仍为 0。

| 预算项目 | 本轮使用 | 批准额度 | 解释 |
| --- | ---: | ---: | --- |
| 全部网络请求 | 50 | 整轮 200；单次检查最多 50 | robots 1、导航 1、资源 37、未分类 XHR/fetch 10、岗位接口 1；计放行请求，含未取得有效响应的请求 |
| 岗位端点 | 1 | 新增 8 | 加历史已知 4 次，累计 5/12；旧账不重置 |
| 其他 host/path | 各 1—2 | 各新增 20 | 此次未触及单端点上限；旧遥测 `/v1/list` 本轮新增 0，旧 6 次保留 |
| 逻辑页面 | 尝试 1、采集 0 | 最多 5 | 仅尝试校园入口；翻页点击 0，另两入口未访问 |

剩余的 150 次整轮请求额度和 7 次新增岗位额度只是未用余额。方案规定“任何额度耗尽即停止”，因此停止整家公司、后续调用 0 次；没有新建 run、重试或续跑。加载 `verifycenter` / `pc_slide.js` 等静态脚本不能单独证明出现了验证码；本次无可用 DOM，无法核实页面验证状态。

这次暴露了计划与工具的适配问题：整轮虽获 200 次请求，但 `inspect_page` 的 schema / 参数校验硬上限只有 50，首次 SPA 检查无法按整轮余额分配足够额度。已向任务“判断 Pi agent 产品定位”（`01a06503-3fc6-7351-9342-88e27bcd48dd`）发送证据和三项通用建议：显式分配检查额度且保持总上限；核查自定义路由是否使资源阻断失效；评估停止时返回已取得的有限脱敏证据。后两项是待维护方核实的建议，不宣称已经修复或已证明根因。

维护任务只做离线修复与验收，不替招聘雷达访问小米。工具更新不解除本轮停止决定；下一次在线调用前必须重新明确可执行的预算分配和剩余历史账本。此次仅追加实验记录与离线回归，没有写业务数据库、生产适配器或云端配置。

本批离线验证：Python 3.13 完整测试 648 项，646 通过、2 项平台跳过。新增回归覆盖各视角请求台账求和、旧岗位计数继承，以及“单次耗尽即停止、整轮剩余额度不续跑”的记录一致性。`manage.py check`、`makemigrations --check --dry-run`、`git diff --check` 均通过。测试通过只证明记录与约束一致，不代表小米采集验收通过。

## 原有浏览器读取方式的后续验证

用户在了解 MCP 中断原因后明确要求“尝试一下，不是说一定要mcp”。此新指令授权改用项目原有的 Playwright 浏览器读取方法进行一次后续尝试；并非因换工具自动解除旧轮停止。沿用未用的 150 次全部网络请求、7 次岗位请求，最多 3 次入口导航加 1 次校园自然下一页，另设更小的 180 秒时间盒。旧实验和 MCP 报告均不改写。

实现：`tools/validate_xiaomi_browser_followup.py` 复用旧脚本的岗位解析、页面限制检测与交互守卫；创建单个全新浏览器 context，三个入口顺序使用。所有放行的 document/script/XHR/ping 都计账，图片/字体/媒体在发送前拦截并单独记录。使用正常 TLS、无代理，不读取个人资料，不导出请求头、Cookie、签名或 CSRF，不重放请求。针对已观察到的 `li.atsx-pagination-next[title="下一页"]`，点击前核验唯一、可见、未禁用、非表单及无危险子控件，实际只点一次。

机器记录：`work/company-expansion-xiaomi-browser-followup-20260907.json`。开始时间 2026-09-07 13:32:51 北京时间，耗时 8.99 秒，本机进程树采样峰值 610.2 MiB；退出码 1，最终因总请求额度耗尽停止。

| 阶段 | 实际取得 | 当前页面/API 声明 | 核验结果 |
| --- | --- | --- | --- |
| 校园首屏 | 10 个唯一岗位 | 768 | 10 个标题全部出现在可见正文 |
| 校园自然下一页 | 10 个唯一岗位 | 768 | 与首屏 ID 无交集，10 个标题全部出现在可见正文 |
| 新零售首屏 | 10 个唯一岗位 | 121 | 与已捕获校园样本 ID 无交集，10 个标题全部出现在可见正文 |
| 实习首屏 | 入口 HTTP 200，无有效岗位响应、无可见正文 | 未知 | 加载中达到预算上限，不能认定无岗位或关闭 |

校园两页的 20 个岗位中，`job_subject.id=7618850506199533866` 出现 4 次，`7659311908839262514` 出现 16 次；可见岗位标签分别对应实习生招聘计划、2027 届校园招聘计划。新零售 10 个样本均为 `job_subject.id=7659311908839360818`，页面标签为 2027 届新零售招聘计划。这是样本中的身份关联线索，未核验境外计划及全部岗位分区。`job_subject.name` 的本地化对象未被通用白名单展开，报告中的 `{}` 表示未保留值，不代表官网名称为空；不能从 `job_process_id` 或 `process_type=2` 自行推导项目含义。

本次新增 150 次放行请求：script 85、XHR 55、ping 7、document 3；另有 10 次图片请求在发送前拦截。加 MCP 的 50 次，批准额度累计为 200/200。岗位端点本次放行 4 次、保留 3 份有效响应；未保留的请求仍计账，已知岗位累计由 5 变为 9/12。总请求余额为 0，未以岗位剩余 3 次单独续跑。

旧 `sample_verdict` 是整轮验收判据：后续任一阶段停止会令 `two_distinct_campus_pages=false`。保留这一原始运行结果，同时用离线 `summarize_evidence` 单独重算已观察事实：唯一安全控件、一次点击、两份 HTTP 200 响应、两页唯一 ID 无交集及全部标题可见，均成立。重算报告 `work/company-expansion-xiaomi-browser-followup-analysis-20260907.json` 明确区分 `campus_natural_next_observed=true` 与 `whole_experiment_complete=false`；全量、项目覆盖、当前可投递和生产准入仍为 false。

结论：原有“浏览器自然访问＋岗位响应解析”方法可用，MCP 可作为辅助工具。下一步业务缺口是实习/境外计划、全量分页、项目分区与独立可投递证明；正式接入仍需单企业生产 ADR，本次没有改动数据库或生产适配器。

本批离线验证：Python 3.13 完整测试 656 项，654 通过、2 项平台跳过；新增后续实验回归 8 项通过，覆盖预算继承、全部资源记账、控件语义和部分观察与整轮验收的区分。项目检查、迁移漂移检查和差异检查均通过。
