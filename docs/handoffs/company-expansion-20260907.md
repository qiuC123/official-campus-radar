# 企业扩容第一批复验 — 2026-09-07

## 结果

用户确认扩大企业数量，第一批为华为、阿里巴巴、字节跳动、小米、中国移动。
本轮新增正式接入 **0 家**；本地仍为 25 家企业、33 个来源，五家目标尚未写入企业库。
发现官网、读到公告或捕获一页岗位，均不等于完成岗位全量采集和正式准入。
未修改业务数据库、云端代码、云端快照或定时任务，也未应用迁移。

## 逐家证据

| 企业 | 本轮实际观察 | 未完成的条件 |
| --- | --- | --- |
| 华为 | 原校园门户 HTTP 200，只识别到三个岗位族/评价原因等配置字典；现行校园首页为动态页面 | 没有获得可验证的实际岗位列表、完整性及项目映射 |
| 阿里巴巴 | 职位搜索捕获 10 条，基线及去签名头重放 200，去 Cookie 后 403；查询含 `_csrf` | 无会话匿名 HTTP 未通过；停止尝试，不移除 CSRF 或复刻会话 |
| 字节跳动 | 浏览器捕获岗位列表 10 条；请求带 `_signature` | 签名请求不重放，尚未验证普通 HTTP 全量采集 |
| 小米 | 旧 `/campus/list` 无岗位接口；现行官网公告接口可匿名读取；官网指向新 ATS，捕获首屏 10 条、接口声明总量 767 | 新 ATS 请求带 `_signature`，0 次重放；767 只是接口声明，不是已采集数量，也未证明多个项目全覆盖 |
| 中国移动 | 官网有独立校招与实习入口，校园页 HTTP 200；探测只识别到无障碍配置数组；本机普通 HTTP 校园页请求出现 `UNSAFE_LEGACY_RENEGOTIATION_DISABLED` | 未取得实际岗位接口；不能为接入而关闭 TLS 校验或开启不安全协商。未在 ECS 复验该 TLS 问题，不能断言云端同样失败 |

### 入口与新发现

- 华为：[现行校园页](https://career.huawei.com/cn/campus-recruitment)，原岗位门户为 `https://career.huawei.com/reccampportal/portal5/campus-recruitment.html?jobTypes=0`。
- 阿里：[岗位页](https://campus-talent.alibaba.com/campus/position)，实际页面批次参数为 `batchId=100000760001`。不保留临时 CSRF 值。
- 字节：[校园岗位页](https://jobs.bytedance.com/campus/position)。不保留签名值。
- 中国移动：[官网](https://job.10086.cn/)、[校招](https://job.10086.cn/personal/campus/)、[实习](https://job.10086.cn/personal/trainee/)。
- 小米：[现行官网](https://hr.xiaomi.com/website/campus.html)，公开接口：
  - `/website/cli/api/hrportal/domestic/campusNews/list`：7 条公告/FAQ，不是 7 个岗位。
  - `/website/cli/api/hrportal/domestic/programs/list`：3 个导航项目，不是 3 个岗位。
  - 官网按钮明确指向 `xiaomi.jobs.f.mioffice.cn` 的 `/campus/`、`/newretailing/`、`/internship/`；本轮只打开了 `/campus/`，后两者不声称已经核验。
  - 公告 ID 13 明示：2027 届；毕业时间 2026-09-01 至 2027-08-31；网申 2026-08-10 至 2027 年 6 月底；算法、软硬件等岗位类别。它能支持后续公告核验，但本轮没有持久化为已核验公告。
  - 新 ATS 岗位端点为 `POST /api/v1/search/job/posts`，分页形态 `offset/limit`，列表路径 `data.job_post_list`、声明总数 `data.count`。不能把签名请求草稿配置为生产源。

## 执行记录与限制

三次开发工具报告生成时间分别为 10:25:25、10:37:43、10:38:49（北京时间）。
R1 访问五家原冻结入口；R2 使用小米现行官网和中国移动官网明确给出的校招入口；R3 跟随小米官网按钮访问新 ATS。
每个目标只有一次开发工具调用；补充入口使用新目标 ID，未覆盖旧失败。
公开网页文本读取仅用于寻找入口；Jina 内容未用作正式公告或岗位证据。

本轮存在两项执行偏差，不能将其描述为完整合规验收：

- 旧工具在阿里第一次 403 后还继续发出了两次梯度请求；现已修复并增加回归测试。
- 中国移动 R1/R2 的不同进程重复重放同一无关无障碍配置端点，累计 10 次，超过该轮单端点 6 次预算；已停止，该结果不用于准入。跨进程预算仍须由操作者按端点台账累计，不能因新进程而重置。后续不再重放这个非招聘端点。

原始临时报告包含无用配置数组和临时参数，完成脱敏汇总后删除；不将原始浏览器请求头、Cookie 或签名纳入仓库。

## 本轮实现

开发工具识别并脱敏查询/嵌套正文中的 `csrf`、`xsrf`，不重放这些请求。
HTTP 401/403/412/429 立即终止梯度，并跳过本次调用内同企业剩余端点和后续页面；其他企业仍可继续。
该修改不改变生产适配器、准入规则或浏览器权限。

验证：Python 3.13 完整测试 637 项，635 通过、2 项平台相关跳过；开发工具相关 61 项通过；项目检查、迁移漂移检查和差异检查通过。

## 下一步

后续更新：用户已明确要求继续项目，2026-09-07 11:44 执行过一次小米浏览器样本实验，因遥测端点预算耗尽提前停止。校园页确实混合正式与实习项目，但翻页和全量仍未证明；具体结果与剩余预算见 `company-expansion-xiaomi-sample-20260907.md`。以下段落保留为该实验之前的交接状态，不再表示小样本验证从未获授权。

优先考虑小米：公告与官方 ATS 归属链已找到，下一缺口是干净浏览器中的岗位翻页、完整性、项目覆盖及当前开放状态。
ADR 0004 只允许中国联通生产浏览器采集，不能直接套用到小米。
如果用户批准小米专项受控浏览器验证，先验证匿名翻页与资源开销，再决定是否新增单企业 ADR 和适配器；不读取个人浏览器资料，不导出或重放签名，不保证验证一定成功。
否则，本批保留为未接入，不扩大生产浏览器范围；更换候选企业名单需由用户明确选择。
