# 小米项目候选与下一次取证 — 2026-09-07

用户在 MCP 合并、重启和实际工具调用验证完成后要求继续项目。本批优先推进小米，先将已有 30 条岗位样本整理为可重复生成的项目候选；没有把样本写入正式数据库，没有新增生产来源。

## 离线结果

来源仍是 `work/company-expansion-xiaomi-browser-followup-20260907.json`，没有重新运行该已耗尽预算的实验。新工具 `tools/prepare_xiaomi_admission.py` 只读取冻结报告，不导入爬虫、不联网、不访问数据库。产物 `work/xiaomi-admission-candidate-20260907.json` 保存来源文件名、UTF-8/LF 归一化 SHA-256、逐岗位投影、页面观察、项目候选和四项准入缺口。

| 稳定项目编号 `job_subject.id` | 页面标签候选 | 样本唯一岗位 | 取自 |
| --- | --- | ---: | --- |
| `7618850506199533866` | 实习生招聘计划 | 4 | 校园首屏 |
| `7659311908839262514` | 2027届校园招聘计划 | 16 | 校园首屏及自然下一页 |
| `7659311908839360818` | 2027届新零售招聘计划 | 10 | 新零售首屏 |

三个编号来自响应字段；标签只作为页面交叉观察的候选。工具只取“招聘项目”到“城市”之间的导航标签，再与岗位标题下一行的卡片信息比对。它不会把标签中出现的年份直接填成招聘对象，也不会自动判断秋招或实习批次的正式准入；各项目 `cohort` 和 `recruitment_phase` 保持空值。

四个“交付体验助理”具有不同岗位 ID，因此全部保留。标题在页面中重复出现时，不能据此把某一地点或单个卡片与 ID 一一绑定；若相同标题的响应记录属于不同项目，则连项目标签提示也不分配。

当前 30 个样本均有项目编号，样本内未发现编号冲突。但校园导航里的“2027届境外招聘计划”没有样本映射；实习入口只有 HTTP 200，未取得有效响应。这些缺口不会被填成空项目、关闭项目或“其他”。校园声明 768 和新零售声明 121 分别保留，不相加为企业岗位总数，因为入口之间的完整集合及交集尚未核验。

## 准入缺口

| 必需证据 | 当前状态 | 下一步需要的证据 |
| --- | --- | --- |
| 官方公告与招聘对象 | 待核验 | 交接中的官网公告 ID 13 只是既有线索；需要冻结官方原文并证明它分别适用于哪些项目 |
| 分页完整 | 未通过 | 校园只取得两页，新零售只取得一页；需要完整遍历、稳定总数和唯一键核验 |
| 项目无遗漏或重叠 | 未通过 | 需要实习及境外项目的身份与入口关系，并以完整岗位集合核对互斥覆盖 |
| 当前可投递 | 待核验 | 独立的官方页面开放状态，不能以岗位库存或 HTTP 200 代替 |

以上沿用 ADR 0005/0006/0009、0010、0011。小米进入生产浏览器采集仍须单企业 ADR；ADR 0004 的中国联通例外不外推。本批没有新增 ADR 或修改现行决策。

## 实习入口方案（提出时记录；已获确认并执行，见后）

精确参数已经冻结在 `work/xiaomi-internship-mcp-proposal-20260907.json`，状态为 `proposed_not_authorized`。该文件保持提出时的状态，不承担后续授权记录；用户随后回复“确认”，批准及执行结果单独写入 `work/xiaomi-internship-mcp-run-20260907.json`，不改写旧实验。

- 只执行一次 `crawl_site(extraction_mode="browser_json")`，从已知实习入口 `/internship/` 自然加载。
- 最多两页、一次初始导航、一次下一页点击；候选 CSS 来自同一 ATS 的校园实测，实习页须重新通过工具的唯一性、可见性、语义和非表单检查，否则停止。
- 申请新增全部网络请求 200 次；与之前已用的 200 次相加，上限为 400，不将旧轮重新记为零。所有放行资源、失败和在途请求均计账。
- 岗位端点只分配原台账剩余的 3 次，已知累计保持不超过 12；其他端点本次申请各新增最多 20 次。保留原端点历史，新分配和新消耗单独累计。
- 时间盒为 900 秒，与当前 MCP 配置的 15 分钟任务时限一致。任何预算到限、超时、访问限制、验证码/滑块/登录要求、响应或身份异常、重复/不变页、总数冲突均停止，不自动重试或启动第二个调用。
- 白名单为岗位 ID、标题、项目编号及招聘性质；项目编号本轮设为必填。只读取自然响应，不重放签名或 CSRF 请求，不调整网站分页参数。
- 本轮目标仅为补齐实习入口样本并与已有校园样本比较。两页样本不证明全量、独立可投递状态或生产准入。四家其他企业继续排在小米专项之后。

## 复现

```powershell
py -3.13 tools/prepare_xiaomi_admission.py
py -3.13 tools/prepare_xiaomi_admission.py --write
py -3.13 -m unittest tools.tests.test_prepare_xiaomi_admission -v
```

默认命令只输出摘要；`--write` 写入确定性的离线候选文件。两者都没有在线执行入口。

新增 8 项离线测试通过：覆盖同名不同 ID、样本与准入隔离、缺失项目保留、跨入口项目冲突、同名跨项目歧义、损坏身份和多响应拒绝、默认无写入与来源哈希、拟议预算继承。Python 3.13 全量回归 669 项，667 通过、2 项平台跳过；`manage.py check`、`makemigrations --check --dry-run` 和 `git diff --check` 通过。本批新增官网请求为 0，追加额度尚未获确认。


## 确认后执行：首屏响应异常停止

用户明确回复“确认”后，仅执行了方案中的一次 `crawl_site` 调用，使用实际已重连的 MCP；不是单独启动服务的协议模拟。调用前以独占创建方式写入运行预留，避免误将方案再次执行。参数与冻结方案逐项相同，MCP 配置任务时限核对为 900 秒。

- 时间：2026-09-07 15:14:47—15:14:50（北京时间，工具时间戳精度为秒），约 3 秒。
- MCP run：`20260907-071447-593708`。
- 结果：`stop_reason=response_http_error`、`completeness=partial`、`complete=false`；完成页数和记录数均为 0，声明总数未知。
- 只有一次入口导航、一次岗位请求，没有取得首个有效页面，因此没有下一页点击。MCP 协议调用 `isError=false` 只表示正常返回了运行结果，不表示采集成功。
- 新增请求 57/200：robots 1、导航 1、资源 34、未分类接口 20、岗位接口 1；另有 4 次图片请求在发送前阻断。总请求及端点、用途、method+endpoint 三种计数相互一致。
- 加之前的 200 次，扩展轮次累计 257/400、未使用 143；岗位已知累计 10/12、未使用 2。本次授权只有一个调用且禁止重试，余额不构成继续调用许可。没有使用旧别名、原脚本或新进程补跑。

机器证据：`work/xiaomi-internship-mcp-response-20260907.json` 保存精确工具结果和调用前后时间；`work/xiaomi-internship-mcp-run-20260907.json` 保存用户确认、冻结方案哈希、调用参数、旧台账和累计台账。已核对 MCP 运行目录的持久化摘要与实际返回逐项相同，并记录摘要 LF 归一化哈希。没有取得 `page-NNNN.json` 岗位投影文件。

本次真实调用时的工具在 `requestfinished` 回调中，把 `request.response()` 缺失和非 2xx 状态都记为 `response_http_error`，没有保留状态码；此次 `errors.jsonl` 也为空。因此不能区分响应对象缺失、重定向、4xx 或 5xx，不能宣称观察到了 403、验证码、封禁、空岗位或项目截止，也不能把此次失败定性为预算不足。

已按用户指定的协作路径，将问题发送给 MCP 维护任务“判断 Pi agent 产品定位”（`01a06503-3fc6-7351-9342-88e27bcd48dd`），限定离线核查与小范围诊断修复：保留有界 HTTP 状态/响应缺失/阶段/端点事实，检查访问限制与普通错误的优先级，不记录查询值、请求头或正文，不通过修改 TLS、代理、UA 或重试来改变访问结果。旧运行的缺失状态码不能由新代码补写，原始失败保持冻结。

实习入口的两页样本目标未完成，原有 30 条样本与三个项目候选保持不变；全量、境外项目、公告及独立可投递证据仍待补齐，没有新增生产准入或数据库/云端修改。本次不再提出下一轮请求，先把诊断信息缺口处理清楚。

本批雷达回归：新增 3 项检查确认授权与冻结参数一致、57 次新请求与既有 200 次逐端点相加、未用余额不允许续跑，以及 HTTP 诊断缺失不能推出封禁/关闭/准入。Python 3.13 全量 672 项，670 通过、2 项平台跳过；`manage.py check`、迁移漂移与差异检查通过。

维护任务已回报离线代码核查结论：旧状态码不可追溯；先前普通错误可能遮住后来确认的访问限制。其修复方案已确认，拟追加最多两条脱敏错误事实、区分响应对象缺失，并保证已确认的访问限制优先。该 MCP 修复由指定任务继续执行，本文收口时尚未收到最终实现与全量验收结果，因此不宣称已经修好，也不要求通过新采集验证。


## MCP 诊断修复已验收（离线）

维护任务随后完成最小修复，仅修改 MCP 项目的 `browser_json.py`、新增 `test_browser_json_errors.py` 并更新 `OBSERVE_BROWSER_JSON.md`。工具入口/输入、请求预算、TLS、UA、代理和公司台账均未变更。新运行返回及持久化摘要增加 `error_facts`，最多保留第一条错误和第一条不同的访问限制事实；同样写入 `errors.jsonl`，路径通过 `artifacts.errors` 返回。

诊断字段只有稳定错误码、阶段、页号、无查询值端点、实际 HTTP 状态或 null，以及响应对象状态。缺失对象为 `response_object_missing`，取对象异常为 `response_lookup_failed`；已确认的 401/403/412/429 或访问墙始终以 `access_restricted` 结束，较早普通错误事实仍保留。没有查询值、异常全文、请求头、响应头或错误正文持久化，停止后不读取后续正文。

主任务独立运行新增 15 项错误回归全部通过，检查了正常/反向/迟到事件、普通错误后出现访问限制、零额度无伪造错误、既有页面保留和脱敏。随后启动全新 stdio 服务，以 `.invalid` 地址及零额度调用统一入口；结果 `error_facts=[]`、`artifacts.errors` 存在、请求 0，返回与落盘摘要一致。此处没有使用公司地址或执行新的实习采集。

主任务还用上一批记录的摘要 SHA-256 核对历史 run `20260907-071447-593708`：摘要完全一致，旧 `errors.jsonl` 仍为空。该次真实异常的状态码没有恢复，也不能由此次修复推断。机器核验记录为 `work/xiaomi-mcp-error-diagnostics-verification-20260907.json`，包含实际 stdio 输入/返回、修复文件哈希、旧摘要哈希和主任务/维护任务各自的验证来源。

维护任务回报全量 Python 119/119、TypeScript 5/5 和类型检查通过，退出码均为 0；主任务没有用上述计数代替自己的独立检查。当前已启动的 MCP 进程可能仍持有旧代码，下次已获授权的在线调用前应重新连接；本次接收修复不产生新的公司调用授权。

本次接收修复的雷达验收：新增一项冻结报告回归，确保新诊断来自零网络检查且不改写旧异常；Python 3.13 全量 673 项，671 通过、2 项平台跳过。项目检查、迁移漂移和差异检查均通过。
