# Phase 02 H2 浏览器验收 / Cycle 02

日期：2026-08-26

状态：H2 已通过

用户确认：2026-08-26，用户在 Django Mock 预览页完成浏览器验收后明确回复“确认”。

## 本 Cycle 验收对象

- 路由：`GET /preview/phase-02/`，仅在 `DEBUG` 模式开放。
- 页面使用独立 Mock ViewModel，不查询正式业务表，不访问企业网站。
- 已完整同步用户确认的 Cycle 08 高密度横向表格、多选筛选、分类徽章配色和批次级手动进度。
- 管理员运维区只在正式页登录管理员后显示；开发预览中明确标注“仅管理员可见”，用于验收正常、漏跑和来源失败三种状态。

## 技术检查

- 1280×720 浏览器实测可用；表格维持 1580px 最小宽度并主动提供横向滚动，符合“桌面 1280px 以上完整可用”的约定。
- 表格固定显示 12 列：公司名称、公司类型、所属行业、招聘类型、招聘对象、工作地点、岗位、投递进度、更新时间、投递截止、相关链接、批次官网。
- 公司类型、招聘类型和投递进度为勾选式多选；地点按省份勾选，选择第 5 个后其余未选项自动禁用。
- 招聘对象覆盖 2024～2028 届及实习生；外资和中外合资分开。
- 一行最多显示 5 个代表岗位；点击岗位区域后，7 个模拟岗位全部在当前页面展开。
- 批次进度改为“已面试”后出现模拟保存提示；预览刷新不写数据库。
- 筛选提交后保留重复 `city` 参数，查询结果正确收敛到匹配批次。
- 预览“投递”和“官方页”均被拦截，页面不跳转到任何企业网站。
- 未知截止时间只显示“未说明”；正式首页空库仍显示“尚无已准入的真实校招岗位”。
- `manage.py makemigrations --check --dry-run` 无待生成迁移；`manage.py check` 无问题；全量 254 个测试通过。

## 用户 H2 确认项

- [x] 页面整体密度和视觉可接受
- [x] 横向表格与滚动方式可接受
- [x] 公司类型、招聘类型、投递进度多选可接受
- [x] 省份最多同时选择 5 个的交互可接受
- [x] 代表岗位与展开全部岗位的方式可接受
- [x] 批次级进度的选择和保存提示可接受
- [x] 管理员运维区与普通招聘信息的层级清楚

H2 Cycle 02 已正式通过。后续如改变已确认的字段或交互，应创建新的变更 Cycle，不覆盖本记录。
